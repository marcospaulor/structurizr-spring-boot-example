structurizr:
  workspace:
    id:  # ← MUDE pelo seu ID real (crie workspace grátis em structurizr.com)
    name: StructurizrExample  # nome do seu modelo
    description: Exemplo baseado no tutorial Catalysts com Spring Boot + Structurizr
    key:  # ← MUDE pela sua API key
    secret:  # ← MUDE pelo seu API secret
  # Opcional: criptografia client-side (como no tutorial)
  # encryptionPassphrase: sua-senha-aqui